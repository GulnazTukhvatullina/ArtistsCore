@using ArtistsCore
@{
    ViewData["Title"] = "Page";
}
@model List<Artist>

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
<br/>

<div class="row">
    @foreach (var command in Model)
    {
    <div class="col-md-4 mb-5">
        <div class="card h-100">
            <div class="card-body">
                @*<div>
                    <img class="card-img-top" width="300" height="300" src="@command.Image"
                         alt="@command.Name" />
                </div>*@

                <div>
                    Фамилия: @command.Surname <br />
                    Имя: @command.Name <br />
                    Отчество: @command.Middle_name <br /> <br />
                </div>
                @*<div class="text-center">
                    <a class="btn btn-danger" asp-controller="Commands"
                       asp-action="Remove" asp-route-id="@command.ID" role="button">
                        Remove
                    </a>
                    <a class="btn btn-primary" asp-controller="Commands"
                       asp-action="Update" asp-route-id="@command.ID" role="button">
                        Update
                    </a>
                </div>*@
            </div>
        </div>
    </div>
    }
</div>
